# Base URL of the backend API. For a physical device, use your machine's LAN
# IP instead of localhost (e.g. http://192.168.1.42:4000) — the device can't
# resolve "localhost" as your dev machine.
EXPO_PUBLIC_API_URL=http://localhost:4000
